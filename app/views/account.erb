<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
      <h2>User Name: <%= Helpers.current_user(session).username %></h2><br>
      <h2>Balance: <%= Helpers.current_user(session).balance%></h2><br>
      <a href="/logout">Log Out</a>
    <% else %>
      <h2>Please <a href="/">Log In</a></h2>
    <% end %>
  </body>
</html>